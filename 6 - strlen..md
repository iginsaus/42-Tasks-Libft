Devuelve la longitud de una cadena, es decir, el número de caracteres en la cadena.

De las páginas man - SYNOPSIS:
```
#include <string.h> 
size_t strlen(const char *s);
```

[[strlen.pdf]]

**NOTA**: La función `strlen()` devuelve el número de caracteres de una cadena, excluyendo el carácter NULL.
[[6-strlen.pdf]]
