Es una función similar a la anterior; en lugar de copiar, concatena src a dst.

De las páginas man - SYNOPSIS:
```
#include <string.h>
size_t strlcat(char *restrict dst, const char *restrict src, size_t dstsize);
```
